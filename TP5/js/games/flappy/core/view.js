export class View {
    /**
     * @param {CanvasRenderingContext2D} ctx  
     */
    constructor(ctx) {
        this.ctx = ctx;
    }

    draw(model) {
        // dibuja el modelo en el canvas
    }
}